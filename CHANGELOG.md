# 更新日志

## v0.1.4 — 2026-09-20

**游戏更新自动补译（重点）**

- 移除了在“扫描之前就判断游戏已含中文 tl 直接返回”的快速分支；
  现在改为先扫描/提取再与 `tl/<语言>` 中已有译文做文本级 diff：
    - 现有译文已覆盖 → “汉化已是最新”，0 次 API 请求；
    - 现有译文仅覆盖部分 → **只翻译新增 / 改动部分**，旧译文 100% 保留；
    - 已删除剧情 → 不再写入新文件，旧的 translate 块不会重新生成。
- 判定基于 identifier 摘要段（md5 前 8 位，由原文决定）：同原文 → 摘要相同 →
  沿用旧译文；原文被修改 → 摘要变化 → 视为新文本并重译。同时兼容重复后缀
  `…_digest_N` 的形式（同文本多次出现）。

**保护机制**

- `tl/<语言>` 目录里已有 `.rpy` 但无法解析为标准 translate 块（如官方译文
  打包在 `.rpa`、或目录内容异常）时，跳过本次汉化并保留原目录，避免误覆盖。

**代码优化**

- 新增 `extract.decode_say_string`：把 `encode_say_string` 的反向还原 + `@@pN@@p`
  编译位置标记剥离集中到一个公共函数。`pipeline` 与 `verify` 共享，不再有
  两份不一致的实现。
- `pipeline.run_pipeline` 中 5.2 更新检测与 7 步组装共用同一份 `_parse_existing_tl`
  结果，避免对同一目录解析两次。
- `engine._detect_rpa_archives` 用显式 `seen`/`unique` 取代 `seen.add` 副作用
  短路写法，提高可读性。
- 删除了局部 `pipeline._ART_PAT`、`pipeline._decode_say`（逻辑并入共用实现）。
- GUI 窗口标题与主标题加入版本号 `v0.1.4`，便于确认当前运行版本。
- `__init__.py` 显式 `__version__ = "0.1.4"`、`APP_NAME` 不变。

**维护**

- `.gitignore` 补充排除 `_gui_worker_err.txt`、`*.log`、`*.err` 等本地临时日志；
  仓库历史中未跟踪的临时日志文件从工作区移除，不再随提交入库。

## 历史

- **v1.0.x 系列**：早期开发迭代号。
- **v0.1.4** 起作为正式对外版本号，沿用语义化版本约定。