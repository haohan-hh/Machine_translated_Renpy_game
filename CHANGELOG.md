# 更新日志

## v0.1.8 — 2026-09-24

**补漏查缺 + 设置界面字体选项**

- **补漏查缺（audit.py，新模块）**：翻译完成后自动扫描 `tl/<语言>/` 全部
  翻译文件，分类检测三类残留问题：
  1. `untranslated` 未翻译残留（译文与原文相同，且原文含成串英文单词）；
  2. `mixed` 中英参半（译文含目标语言文字，同时残留 ≥3 个连续英文单词；
     单个英文单词视为保留的人名/术语，不误报）；
  3. `wrong_lang` 目标语言不正确（译文含其他语言文字——假名/韩文/西里尔/
     泰文/希腊文，或整句仍是英文而与原文不同——模型「改写」而非「翻译」）。

  判定只在原文确实是外文（不含目标语言文字）时进行，游戏自带部分汉化
  的条目自动跳过；`{...}` 标签与 `[...]` 插值先剥离再判定。检测结果写入
  `tl/<语言>.补漏查缺报告.txt`（含 文件:行号 定位 + 原文/译文对照，供
  人工逐条核查）；同时把问题项原文合并进「未翻译报告」（按文本去重）——
  **再次点击「开始汉化」时增量模式会自动重译这些文本**，形成「扫描 →
  排队 → 重译 → 再扫描」的补充修正闭环。全部正常时自动删除旧报告。

- **GUI：手动「补漏查缺」按钮**：进度栏旁新增按钮，对已翻译的游戏单独
  执行扫描（不动翻译、只出报告+排队）；与翻译任务的互斥/忙碌状态由
  `_set_busy` 统一管理。翻译完成后 `run_pipeline` 的结果汇总
  （`result.message`）会列出各类问题条数、报告路径与重译队列状态。

- **设置界面字体选项**（patcher.py）：字体补丁（zz_cn_font.rpy）现在
  提供 `_zz_font_choices` / `_zz_set_font` / `_zz_font_select`：
  - init -1 探测系统微软雅黑（`msyh.ttc` / `msyh.ttf`，只把 Windows
    Fonts 目录加进 `config.searchpath`，不复制文件）；
  - init 999 定义 `_zz_set_font(f)`（gui 变量 + 默认样式 + 全部命名
    样式 + `font_replacement_map` 一站式应用）与 `_zz_font_select`
    （记住 `persistent._zz_font_choice` 并即时 `renpy.style.rebuild()`
    + `renpy.restart_interaction()`——引擎自身惯用法，见
    renpy/common/00stylepreferences.rpy）；
  - 启动时按上次选择应用字体（`persistent` 未设置时返回 None，安全）。

  设置界面（zz_language_ui.rpy）新增「字体」单选：**汉化默认字体 /
  微软雅黑**（探测到才显示后者）。双层防崩：注入仅在字体补丁确实就位时
  进行（`_font_ui_available` 门控 4 种组合测试通过）；界面按钮全部包在
  `if _zz_font_choices:` 内且 helper 在补丁缺失时兜底空表——最坏情形只
  多渲染一个标题，不会 NameError。切换即时作用于游戏内所有文本（GUI
  变量 / 命名样式 / 屏幕内联 `font=` 元素经替换映射全覆盖）。显示名
  `Font` → `字体` 一并翻译。

## v0.1.7 — 2026-09-23

**完成时清理 + named menu 进 ID**

- **完成时清理遗留文件**（pipeline.py）：当本次翻译任务完全成功（无回退原文、
  无错误）时，自动删除 3 个上次运行留下的文件——`[未知语言].未翻译报告.txt`、
  `.[未知语言].暂停标记.txt`、`[未知语言].运行时缺失报告.txt`。让"报告/标记是否
  存在"准确反映"是否还有未完成内容"，避免上次运行的噪音被下一轮误判为未完成
  （运行时缺失报告已被 `build_missing_fix()` 在本轮消费过）。`result.paused=True`
  或有未翻译条目时一律不动。

- **`menu name:` 也进 ID 路径**（extract.py）：之前 dispatcher 只让 `label name:`
  触发 label 切换，遗漏了 Ren'Py 里「命名的 menu」——`menu chapter_L3.distraction:`
  等价于一个隐式 label，对话 / 选项的 identifier 以此为前缀。上一版 v0.1.6 的
  sub-label 修复对 menu 无效，故仍有 ~1170 条 ID 错位（`chapter_L3_distraction_*`
  / `chapter_M3_ritual_*` 等）。修复：dispatcher 同时识别 `label` / `menu`
  关键字，调同一个 `_handle_label` 处理。实测游戏《Where The Demon Lurks》本轮
  命中率 89.8% → **95.2%**，其余 4.8% 来自 `extend` / 动态 label 等边缘场景，
  留作后续。

- 本轮 "tl/schinese 下新文件"分析：`.schinese` 下 `zz_language_display.rpy` 是
  `ensure_language_name()` 主动生成的（翻译"Language/English/简体中文"等显示名），
  由 patcher.py 维护，无需修复；游戏运行时生成的 `schinese.运行时缺失报告.txt`
  已被 `verify.py.build_missing_fix()` 自动消费补译，覆盖率比 v0.1.6 提升 5.4
  个百分点，无需额外处理。

- **界面版本号与软件版本号同步**（gui_main.py）：主界面标题文本硬编码为
  `v0.1.5`、窗口标题栏硬编码为 `v0.1.7`，与实际版本 `__version__` 三处各写一份，
  必然不一致。改为单一数据源：`_APP_TITLE = f"{APP_NAME} v{__version__}"`，
  窗口标题栏与界面标题文本（`AppTitleText`）都从这里取值，不再各自硬编码。
  本次仅做同步，不新增版本号、不新增版本显示位置。

- **模型选择下拉列表**（gui_main.py）：模型输入框（ModelBox）右侧加 ▼ 按钮
  （`ModelMenuBtn` + `MenuFlyout`），点击展开所有「保存设置」时记录过的模型
  配置，按模型名去重（最多 20 条），点击条目即把该条目保存的 url / key / model
  写回界面（不自动落盘，下次「保存设置」才落盘）。空列表显示「（暂无已保存的
  模型，点击「保存设置」加入）」。配置 schema 扩展：`saved_models` 字段；
  旧配置无此字段时退化为「当前一项」，向后兼容。

- **修复：下拉列表导致软件无法启动**（gui_main.py，紧急）：上一版误把
  `MenuFlyout.Items` 当成 .NET 风格集合，用了 `items.Count` —— 它是 WinRT 的
  `IVector<T>`，取长度用 `Size`、清空用 `Clear()`、追加用 `Append()`，
  **没有 `Count`**。该 `AttributeError` 在 `OnLaunched → _load_settings →
  _rebuild_model_menu` 链路上抛出，逃出 WinUI 回调后**直接终止进程**，表现为
  「双击 .bat 后命令窗闪过、主界面起不来」。修复要点：
  - 改用 `Append`（经 `_vector_append` 做 Append/append/Add 命名兼容）；
  - 每次**新建**一个 MenuFlyout 重新挂到按钮，彻底不再需要
    `Count`/`Size`/`Clear`/`RemoveAt`；
  - 条目回调改用**闭包**捕获配置，不再走 `Tag`（其类型是 `IInspectable`，
    赋 Python dict 在 win32more 下不可靠）；闭包存入 `_menu_handlers` 防 GC；
  - 整个重建过程 try/except 兜底——下拉只是便利功能，任何异常都不得再拖垮
    启动。
  实测（带探针启动真实 GUI）：`_rebuild_model_menu` 正常执行、`Append` 两次
  调用均返回成功、进程稳定运行、无异常输出；并用「10 秒自动退出」脚本验证了
  旧配置（无 `saved_models` 字段）能被正确迁移为 1 条。

## v0.1.6 — 2026-09-23

**系统性修复 4 类问题（针对 Ren'Py 发行版游戏常见缺陷）**

针对游戏《Where The Demon Lurks》实测发现的 4 类问题逐一修复；多数是汉化工具
与 Ren'Py 打包发行版共存时的系统性问题，对所有其他 Ren'Py 游戏同样受益。

- **抽取器只读 .rpa 也能工作**（Issue 1 根因）：

  Ren'Py 发行版常把所有脚本打包进 `archive.rpa`，游戏目录只剩缓存与资源。
  上一版只检查「是否有散落 .rpy」来决定要不要拆包——但工具自己生成的
  `zz_*.rpy` 补丁也算「散落 .rpy」，导致第二次运行以为游戏没归档，
  `screens.rpy`（含偏好设置文本）等 UI 源脚本永远抽不到。
  
  修复：忽略 `zz_*` 前缀的散落脚本后，再判断是否有真正的游戏脚本。
  没有时才走 archive.rpa 拆包。

- **sub-label 跟踪进 ID 路径**（Issue 3 根因）：

  抽器碰到 `label .arcade2:`（子标签）时直接跳过、不改当前 label，导致
  抽取器把 `chapter_L3.arcade2` 块里的对白归到 `chapter_L3_` 下，runtime
  拿 `chapter_L3_arcade2_xxxx` 去 tl/ 里查当然查不到——11460 条全「缺失」。
  
  修复：sub-label 改为追加到当前 label（Ren'Py 的实际语义），identifier 生成
  时 `.` 替换为 `_`。实测：`chapter_L3.arcade2` → `chapter_L3_arcade2_<digest>`，
  与运行时报告完全匹配。

- **`font_replacement_map` 全局字体替换**（Issue 2 收尾）：

  上一版已经覆盖 `gui.*_font` 与所有命名样式，但屏幕里**显式**写
  `font="font/CarterOne-Regular.ttf"` 的（属性优先级最高）仍漏掉。
  改为枚举 `game/font` 与 `game/fonts` 下的所有非 CJK 字体，写进
  `config.font_replacement_map[(原字体, 粗, 斜)] = (cn_font, 粗, 斜)`，
  替换在字体加载层完成，所有用到原字体的元素都会显示中文字体。
  代价：英文会失去原游戏的字体风格，换成中文美观度优先。

- **「提示词镜像」拒收判定**（Issue 4 收尾）：

  实测某些原句（典型为 "I agree with the ..."）会让模型直接把系统提示词
  的中译本当成译文返回（玩家会看到工具自己的指令出现在对白里）。
  在 `_looks_like_meta_reply` 里加一组 prompt-echo 标记（「非交互式 /
  JSON 数组 / 自动化 / 打招呼 / 批量翻译任务」等游戏对白里绝不可能出现的
  短语），命中即拒收、退化为原文本并计未翻译报告。

**已知局限（不在本版修复范围）**

- `imagebutton auto "gui/navigation_buttons/start_%s.png"` 这类**主菜单按钮**
  的英文是烤进 PNG 里的（screens.rpy 里能查到引用，但文本不在 .rpy 源里），
  文本工具无能为力；解决途径只有改图或改屏加 textbutton 覆盖。
  这次的抽取/字体修复不影响这一类。

## v0.1.5 — 2026-09-22

**暂停与断点续译（重点）**

- GUI 新增「⏸ 暂停」按钮：置位信号后，翻译线程在当前请求完成的间隙
  立即停止（每批前后、每轮重试前均检查），已发出的请求不被掐断。
- 已译条目实时落盘 `tl/.{语言}.汉化进度.json`（源指纹校验，游戏更新后
  旧缓存自动作废），暂停时强制刷新。
- 暂停时生成 `tl/.{语言}.暂停标记.txt`：按提取顺序列出全部未完成条目
  （文件:行号 原文）与完成/剩余统计，未完成段落可视化。
- 重启程序再点「开始汉化」：自动加载进度缓存并识别暂停标记，按原顺序
  只译剩余条目；续译完成后标记与缓存自动删除。批量翻译无跨请求状态，
  续译与暂停前译文风格一致（同一套提示词与术语保护）。

**本地模型连接修复**

- 修复 `localhost` 解析出 IPv6(`::1`) 优先而本地服务只监听 IPv4 时，
  误报“目标计算机积极拒绝”(WinError 10061)：改为逐个尝试全部解析地址，
  全部失败后短暂重试一轮。
- 连接被拒/超时/权限拦截等错误分类给出可操作中文排查建议（启动 Ollama /
  LM Studio、端口对照、建议改用 `127.0.0.1`）。
- 测试连接超时 30s → 120s，本地模型首次加载不再被误判失败。
- 新增“静默不翻译”检测：译文与原文完全相同且占比高时警告模型可能不支持
  该语言方向（如 sakura 仅支持日→中，英文游戏会原样返回）。
- **提示词风格按模型实际返回行为自动适配**（不依赖模型名称 / 厂商 / 是否本地
  部署）：默认用详细提示词；若模型返回的不是可解析的 JSON 数组（部分未经过
  指令微调的机器翻译模型的典型表现），自动改用精简英文提示词重试，成功即
  记住并长期沿用——可稳定输出正确 JSON 数组并保住占位符，翻译不中断。
  正常模型零额外请求、行为完全不变；也可用 `compact_prompt` 手动锁定风格。
- 进度百分比旁显示预计剩余时间倒计时（指数平滑 + 起步保护 + 进度单调不减）。

**译文质量把关（防乱码 / 防聊天语）**

弱模型（尤其纯机器翻译模型）会用「聊天」代替翻译，旧版本会把这类回复当成
译文写进游戏。现在在写入前做四重把关，宁可让该条回退原文并计入未翻译报告，
也不让垃圾进游戏：

- **失败批次自动折半**：批量请求失败（条数不符 / 非 JSON / 占位符丢失）时，
  把该批对半切开重试，最多折 4 层，最末层再退化为逐条翻译。弱模型在小批量下
  成功率明显回升，比反复重发同样大的批次更省请求。
- **剥掉译文外壳**：模型可能返回 `["…"]`、`{"response": "…"}`、```` ```json ````
  代码块或整体带引号，现在统一剥成纯译文，避免游戏里出现 `["译文"]`。
- **残留标记拒收**：模型有时会把系统提示词里的占位符示例 `@@p0@@` 抄进译文
  （且常抄错成 `@p0@@p`、`<｠@p0@@p｠>`），这类译文一律拒收。
- **聊天式回应拒收**：命中「请提供 / 需要翻译 / 请问有什么可以帮」等典型客服
  措辞，或长度远超原文（>4 倍 + 80 字）的回复，判为未翻译而非译文。

**本地模型吞吐与提示词**

- **本地服务自动并发**：端点指向本机（localhost / 127.0.0.1）时自动并发 4 路
  请求，让 GPU 同时处理多个批次（实测单请求串行时 GPU 利用率仅 ~30%）；
  云端服务保持串行以免触发限流。
- **精简提示词加固**：开头声明「自动化、非交互的批量任务，禁止问候/提问/
  解释」，并明确「禁止自行编造占位符标记」——针对实测中模型抄写提示词里
  `@@p0@@` 示例进译文、以及短输入触发聊天模式两类失败。
- 附：本地推理模型建议把 `repeat_penalty` 设为 1（JSON 结构化输出中重复的
  小 token 会被重复惩罚，导致占位符丢失），`num_batch` 用默认 512 而非 128。

**吞吐优化（自适应批大小 + 并行槽）**

- **批大小自适应（AIMD，类拥塞控制）**：部分模型在大批次上会生成几条就提前
  停止。现在会记住「近期稳定成功的批大小」，超限的组在发请求前预先折半，
  成功后缓慢放大探测更大批次——不再反复发起注定失败的大批请求。
- **格式错误立即折半**：非 JSON/条数不符等结构性错误重发同一批次必然再失败
  （内部已试过两种提示词），现在直接跳过重试进入折半，只有网络类瞬时错误
  才原样重试。
- **单条请求输出上限**：按原文长度推算 `max_tokens`，聊天式长回复被提前截断，
  省下等待整段无效生成的时间；正常译文远短于上限，不受影响。
- **模板泄漏检测**：模型把自家对话模板的特殊 token（如 `<｜hy_User｜>` 及
  残缺变体）抄进译文时，批量与单条路径均拒收。
- 附：本地并行推理建议设置 `OLLAMA_NUM_PARALLEL=4`、`OLLAMA_FLASH_ATTENTION=1`、
  `OLLAMA_KV_CACHE_TYPE=q8_0`（KV 显存减半后 Ollama 才会开出并行槽）。

## v0.1.4 — 2026-09-20

**游戏更新自动补译（重点）**

- 移除了在“扫描之前就判断游戏已含中文 tl 直接返回”的快速分支；
  现在改为先扫描/提取再与 `tl/<语言>` 中已有译文做文本级 diff：
    - 现有译文已覆盖 → “汉化已是最新”，0 次 API 请求；
    - 现有译文仅覆盖部分 → **只翻译新增 / 改动部分**，旧译文 100% 保留；
    - 已删除剧情 → 不再写入新文件，旧的 translate 块不会重新生成。
- 判定基于 identifier 摘要段（md5 前 8 位，由原文决定）：同原文 → 摘要相同 →
  沿用旧译文；原文被修改 → 摘要变化 → 视为新文本并重译。同时兼容重复后缀
  `…_digest_N` 的形式（同文本多次出现）。

**保护机制**

- `tl/<语言>` 目录里已有 `.rpy` 但无法解析为标准 translate 块（如官方译文
  打包在 `.rpa`、或目录内容异常）时，跳过本次汉化并保留原目录，避免误覆盖。

**代码优化**

- 新增 `extract.decode_say_string`：把 `encode_say_string` 的反向还原 + `@@pN@@p`
  编译位置标记剥离集中到一个公共函数。`pipeline` 与 `verify` 共享，不再有
  两份不一致的实现。
- `pipeline.run_pipeline` 中 5.2 更新检测与 7 步组装共用同一份 `_parse_existing_tl`
  结果，避免对同一目录解析两次。
- `engine._detect_rpa_archives` 用显式 `seen`/`unique` 取代 `seen.add` 副作用
  短路写法，提高可读性。
- 删除了局部 `pipeline._ART_PAT`、`pipeline._decode_say`（逻辑并入共用实现）。
- GUI 窗口标题与主标题加入版本号 `v0.1.4`，便于确认当前运行版本。
- `__init__.py` 显式 `__version__ = "0.1.4"`、`APP_NAME` 不变。

**维护**

- `.gitignore` 补充排除 `_gui_worker_err.txt`、`*.log`、`*.err` 等本地临时日志；
  仓库历史中未跟踪的临时日志文件从工作区移除，不再随提交入库。

## 历史

- **v1.0.x 系列**：早期开发迭代号。
- **v0.1.4** 起作为正式对外版本号，沿用语义化版本约定。